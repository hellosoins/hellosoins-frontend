import axios from 'axios';
const API_URL = import.meta.env.VITE_API_BASE_URL;

export const addFormationPraticiens = async() => {
    try {
        console.log("huh");
    } catch (error) {
        console.error(error)
    }
}